basePath: /api/v1
definitions:
  teams.Team:
    properties:
      _id:
        type: string
      createdat:
        type: string
      department:
        type: string
      id:
        type: integer
      name:
        type: string
      tags:
        additionalProperties:
          type: string
        type: object
    type: object
host: localhost:8081
info:
  contact:
    email: bolmidgk@mail.uc.edu
    name: MiriConf
    url: https://github.com/MiriConf/miriconf-backend
  description: The backend API for MiriConf.
  license:
    name: GPL3
    url: https://www.gnu.org/licenses/gpl-3.0.en.html
  title: MiriConf Backend API
  version: "1.0"
paths:
  /teams/list:
    get:
      description: list all teams
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/teams.Team'
            type: array
        "400":
          description: Bad Request
          schema:
            type: string
        "404":
          description: Not Found
          schema:
            type: string
        "500":
          description: Internal Server Error
          schema:
            type: string
      summary: List all teams
      tags:
      - teams
swagger: "2.0"
